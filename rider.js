document.getElementById('rideForm').addEventListener('submit',e=>{e.preventDefault();document.getElementById('msg').textContent='Ride request saved locally for now. Real route pricing will be connected after Firebase is fixed.';});