rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /rides/{rideId} {
      allow read, write: if false;
    }
    match /config/{docId} {
      allow read, write: if false;
    }
  }
}
